<section id="commerce">
    <div class="wrap">
        <div class="logo">
            <img src="{{ vich_uploader_asset(boutique, 'imageFileLogo') }}" alt="{{ boutique.title }}" width="200"/>
        </div>
        <div class ="titre">
            <h1>Boutique {{ boutique.title }}</h1>
        </div>
        {# /////zone produit///// #}
        <div class ="shop-container">
            <div class="produits">
                <div class="boxs">
                    <ul class="boxs">
                        
                        <li class="shadow">
                            <div class="bloc_text">

                                <img src="{{vich_uploader_asset(produit, 'imageFileProduit')}}" alt="{{ produit.title }}" width="400">
                                <h2> {{ produit.title }} </h2>
                                <p>{{ produit.description }}</p>
                                <p>{{ (produit.prix) }}€</p>
                                <div class="box-button">
                                    <input class="inp-btn" type="submit" value="Ajouter au Panier"></input>
                                </div>
                            </div>
                        </li>
                    </ul>
                </div>
                <div class="inscriptions">
                    <input class="inp-btn" type="submit " value="Valider le Panier">
                </div>
            </div>
            <!-- --------nav bar d'information-------------- -->
                    {% include "nav/nav_partials/_nav.html.twig" %}
                <!-- /////////////////////////////////////////////////// -->
        </div>
        <div class="pagination">
            <ul>
                <li>
                    <a href="{{path('app_index_boutiques')}}">Retour Boutique</a>
                </li>
            </ul>
        </div>
        
    </div>
</section>
{# ////////////////////////////////////////////////// #}