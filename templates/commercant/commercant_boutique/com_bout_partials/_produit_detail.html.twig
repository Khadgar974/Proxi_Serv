<hr class="hr-text">
<section id="commerce">
    <div class="wrap">
        <div class="logo">
            <img src="{{ vich_uploader_asset(app.user.boutique, 'imageFileLogo') }}" alt="{{ app.user.boutique.title }}" width="200"/>
        </div>
        <div class ="titre">
            <h1>Boutique {{ app.user.boutique.title }}</h1>
        </div>
        {# /////zone produit///// #}
        <div class ="shop-container">
            <div class="produits">
                <div class="boxs">
                    <ul class="boxs">
                        
                        <li class="shadow">
                            <div class="bloc_text">

                                <img src="{{vich_uploader_asset(produit, 'imageFileProduit')}}" alt="{{ produit.title }}" width="600">
                                <h2> {{ produit.title }} </h2>
                                <div class="prix">
                                    <div class="dash">
                                        <p>{{produit.prix}}€</p>
                                    </div>
                                </div>
                                <p>{{ produit.description }}</p>
                                <div class="box-button">
                                    <input class="inp-btn" type="submit" value="Ajouter au Panier"></input>
                                </div>
                            </div>
                        </li>
                    </ul>
                </div>
                <div class="inscriptions">
                    <input class="inp-btn" type="submit " value="Valider le Panier">
                </div>
            </div>
            
        </div>
        <div class="pagination">
            <ul>
                <li>
                    <a href="{{path('app_commercant_produits_index')}}">Retour Boutique</a>
                </li>
            </ul>
        </div>
    </div>
</section>
<hr class="hr-text">
{# ////////////////////////////////////////////////// #}